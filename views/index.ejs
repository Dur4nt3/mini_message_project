<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/reset.css">
    <title>Message Board</title>
</head>
<body>
    <h1>Your Messages:</h1>
    <button class="create-new"><a href="/new">New Message</a></button>
    <div class="all-messages">
        <% for (const messageIndex in locals.messages) { %>
            <% const message = locals.messages[messageIndex] %>
            <div class="message-cont">
                <h2>From: <%= message.username %></h2>
                <a href=<%= `/messages/${message.id}` %>>Open</a>
                <p class="message-time">@ <%= message.added.toString().split(' GMT')[0] %> UTC</p>
            </div>
        <% } %>
    </div>
</body>
</html>